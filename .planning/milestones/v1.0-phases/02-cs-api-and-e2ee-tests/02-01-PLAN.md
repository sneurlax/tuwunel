---
phase: 02-cs-api-and-e2ee-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/shadow/Cargo.toml
  - tests/shadow/src/lib.rs
  - tests/shadow/src/scenarios/mod.rs
  - tests/shadow/src/scenarios/common.rs
  - tests/shadow/src/config/shadow.rs
  - tests/shadow/src/config/tuwunel.rs
  - tests/shadow/src/bin/matrix_test_client.rs
autonomous: true
requirements:
  - TEST-05
  - TEST-06

must_haves:
  truths:
    - "matrix-sdk 0.16 compiles in the shadow-test-harness crate with e2e-encryption and rustls-tls features"
    - "A multi-host Shadow config (server + alice-host + bob-host) can be generated programmatically"
    - "Common helpers exist for SDK client construction, registration with UIAA token, and server readiness polling"
    - "The matrix_test_client binary accepts cs-api, e2ee-messaging, and sas-verify subcommands"
  artifacts:
    - path: "tests/shadow/Cargo.toml"
      provides: "matrix-sdk dependency declaration"
      contains: "matrix-sdk"
    - path: "tests/shadow/src/scenarios/common.rs"
      provides: "Shared SDK helpers for all scenarios"
      exports: ["create_sdk_client", "register_with_token", "wait_for_server"]
    - path: "tests/shadow/src/scenarios/mod.rs"
      provides: "Module declarations for scenario implementations"
    - path: "tests/shadow/src/config/shadow.rs"
      provides: "Multi-host topology builder"
      contains: "fn three_host_config"
  key_links:
    - from: "tests/shadow/src/scenarios/common.rs"
      to: "matrix-sdk Client"
      via: "Client::builder().homeserver_url()"
      pattern: "Client::builder"
    - from: "tests/shadow/src/bin/matrix_test_client.rs"
      to: "tests/shadow/src/scenarios/"
      via: "use shadow_test_harness::scenarios"
      pattern: "scenarios::"
---

<objective>
Add matrix-sdk dependency, build the scenario module scaffold with common helpers (SDK client creation, UIAA registration, server readiness polling), extend Shadow config for multi-host topologies (server + alice + bob), and add subcommand stubs to matrix_test_client binary.

Purpose: Foundation that all CS API and E2EE scenario plans build on. Validates matrix-sdk compiles with workspace patches.
Output: Compilable crate with matrix-sdk, scenario helpers, multi-host config builder, and subcommand routing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cs-api-and-e2ee-tests/02-CONTEXT.md
@.planning/phases/02-cs-api-and-e2ee-tests/02-RESEARCH.md

@tests/shadow/Cargo.toml
@tests/shadow/src/lib.rs
@tests/shadow/src/bin/matrix_test_client.rs
@tests/shadow/src/config/shadow.rs
@tests/shadow/src/config/tuwunel.rs
@tests/shadow/src/runner.rs
@tests/shadow/tests/smoke.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add matrix-sdk dependency and scenario module scaffold</name>
  <files>
    tests/shadow/Cargo.toml
    tests/shadow/src/lib.rs
    tests/shadow/src/scenarios/mod.rs
    tests/shadow/src/scenarios/common.rs
  </files>
  <read_first>
    tests/shadow/Cargo.toml
    tests/shadow/src/lib.rs
    tests/shadow/src/bin/matrix_test_client.rs
    Cargo.toml
  </read_first>
  <action>
1. In `tests/shadow/Cargo.toml`, add matrix-sdk dependency with explicit features (per D-01, D-03, research Pitfall 7):
   ```toml
   matrix-sdk = { version = "0.16", default-features = false, features = ["e2e-encryption", "rustls-tls"] }
   ```
   Also add `tracing` and `tracing-subscriber` for structured logging in test client:
   ```toml
   tracing = { workspace = true }
   tracing-subscriber = { workspace = true, features = ["fmt", "env-filter"] }
   ```

2. In `tests/shadow/src/lib.rs`, add `pub mod scenarios;` alongside existing modules.

3. Create `tests/shadow/src/scenarios/mod.rs` with:
   ```rust
   pub mod common;
   pub mod cs_api;
   pub mod e2ee_msg;
   pub mod sas_verify;
   ```
   Create stub files `cs_api.rs`, `e2ee_msg.rs`, `sas_verify.rs` that are initially empty (will be populated by Plans 02-04).

4. Create `tests/shadow/src/scenarios/common.rs` with these functions:

   - `pub async fn wait_for_server(base_url: &str, max_retries: u32, retry_interval_ms: u64) -> Result<(), Box<dyn std::error::Error>>`: Reuse the polling pattern from the existing smoke subcommand (GET `/_matrix/client/versions` in retry loop with `tokio::time::sleep`). Use `reqwest::Client` with `danger_accept_invalid_certs(true)` and 5s timeout. Log each attempt to stderr via `eprintln!`.

   - `pub async fn create_sdk_client(server_url: &str) -> Result<matrix_sdk::Client, Box<dyn std::error::Error>>`: Call `wait_for_server(server_url, 60, 500)` first, then build `matrix_sdk::Client::builder().homeserver_url(server_url).disable_ssl_verification().request_config(RequestConfig::default().timeout(Duration::from_secs(10))).build().await`. Return the client.

   - `pub async fn register_with_token(client: &matrix_sdk::Client, username: &str, password: &str, token: &str) -> Result<(), Box<dyn std::error::Error>>`: Implement the UIAA two-step registration flow (per research Pattern 2, Pitfall 2). First call `client.register()` with username/password. On error, extract the UIAA session from `matrix_sdk::HttpError::UiaaError`, create `AuthData::RegistrationToken` with the session, and retry. Log success to stderr: `eprintln!("{username} registered")`.

   - `pub async fn login_user(client: &matrix_sdk::Client, username: &str, password: &str) -> Result<(), Box<dyn std::error::Error>>`: Call `client.matrix_auth().login_username(username, password).send().await`. Log: `eprintln!("{username} logged in")`.

   - `pub const REGISTRATION_TOKEN: &str = "shadow_test_token";` (matches TuwunelConfig default).
   - `pub const SERVER_NAME: &str = "tuwunel-server";`
   - `pub const DEFAULT_PASSWORD: &str = "shadow_test_pass";`

5. Build-verify: Run `cargo check -p shadow-test-harness` to confirm matrix-sdk compiles with workspace patches. If it fails due to workspace `[patch.crates-io]` entries (hyper-util, event-listener, async-channel), the fallback per research is to use a compatible matrix-sdk git rev or adjust features. Document what worked.
  </action>
  <verify>
    <automated>cd /home/user/src/manymatrix/tuwunel && cargo check -p shadow-test-harness 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/shadow/Cargo.toml contains `matrix-sdk = { version = "0.16", default-features = false, features = ["e2e-encryption", "rustls-tls"] }`
    - tests/shadow/src/lib.rs contains `pub mod scenarios;`
    - tests/shadow/src/scenarios/mod.rs contains `pub mod common;`
    - tests/shadow/src/scenarios/common.rs contains `pub async fn create_sdk_client(`
    - tests/shadow/src/scenarios/common.rs contains `pub async fn register_with_token(`
    - tests/shadow/src/scenarios/common.rs contains `pub async fn wait_for_server(`
    - tests/shadow/src/scenarios/common.rs contains `pub async fn login_user(`
    - tests/shadow/src/scenarios/common.rs contains `REGISTRATION_TOKEN`
    - `cargo check -p shadow-test-harness` exits 0
  </acceptance_criteria>
  <done>matrix-sdk compiles in the workspace, scenario module exists with common helpers for client creation, UIAA registration, login, and server readiness polling.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Shadow config for multi-host topologies and add subcommand stubs</name>
  <files>
    tests/shadow/src/config/shadow.rs
    tests/shadow/src/config/tuwunel.rs
    tests/shadow/src/bin/matrix_test_client.rs
  </files>
  <read_first>
    tests/shadow/src/config/shadow.rs
    tests/shadow/src/config/tuwunel.rs
    tests/shadow/src/bin/matrix_test_client.rs
    tests/shadow/src/scenarios/common.rs
  </read_first>
  <action>
1. In `tests/shadow/src/config/shadow.rs`, add a builder function for the three-host topology (per D-07, D-08, research Pattern 3):

   ```rust
   /// Build a Shadow config with tuwunel-server + alice-host + bob-host.
   /// Per D-07: separate Shadow hosts with own virtual IPs.
   /// Per D-08: deterministic naming conventions.
   pub fn three_host_config(
       tuwunel_bin: &Path,
       client_bin: &Path,
       config_path: &Path,
       data_dir: &Path,
       subcommand: &str,
       stop_time: &str,
       seed: u32,
       alice_start: &str,
       bob_start: &str,
   ) -> ShadowConfig
   ```

   This function creates:
   - `tuwunel-server` host (network_node_id: 0) with the server process starting at "1s", expected_final_state "running", TUWUNEL_CONFIG and TUWUNEL_LOG env vars, shutdown_signal "SIGTERM"
   - `alice-host` host (network_node_id: 0) with client_bin process, args: `"{subcommand} --server-url http://tuwunel-server:8448 --role alice"`, start_time: alice_start, expected_final_state: "exited"
   - `bob-host` host (network_node_id: 0) with client_bin process, args: `"{subcommand} --server-url http://tuwunel-server:8448 --role bob"`, start_time: bob_start, expected_final_state: "exited"
   - General with the given stop_time, seed, model_unblocked_syscall_latency: true, data_directory: Some(data_dir path)
   - Default Network (1_gbit_switch) and HostOptionDefaults (pcap enabled)

   Add `use std::path::Path;` import.

2. In `tests/shadow/src/config/tuwunel.rs`, add `pub allow_encryption: bool` field to `TuwunelGlobal` (default true in `TuwunelConfig::new`). No other E2EE config needed per research.

3. In `tests/shadow/src/bin/matrix_test_client.rs`:
   - Add three new `Commands` variants per D-04:
     ```rust
     /// CS API test: register, login, create room, send message, sync
     CsApi {
         #[arg(long)]
         server_url: String,
         #[arg(long)]
         role: String, // "alice" or "bob"
     },
     /// E2EE messaging: key upload, claim, encrypted message exchange
     E2eeMessaging {
         #[arg(long)]
         server_url: String,
         #[arg(long)]
         role: String,
     },
     /// SAS verification between two devices
     SasVerify {
         #[arg(long)]
         server_url: String,
         #[arg(long)]
         role: String,
     },
     ```
   - Add match arms that build a current_thread tokio runtime (same as Smoke) and call placeholder async functions that just `eprintln!("TODO: {subcommand} for {role}")` and return `Ok(())`.
   - Keep the existing Smoke variant and its handler unchanged (per D-02).
  </action>
  <verify>
    <automated>cd /home/user/src/manymatrix/tuwunel && cargo check -p shadow-test-harness 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/shadow/src/config/shadow.rs contains `pub fn three_host_config(`
    - tests/shadow/src/config/shadow.rs contains `"alice-host"` and `"bob-host"` and `"tuwunel-server"` string literals
    - tests/shadow/src/config/tuwunel.rs contains `pub allow_encryption: bool`
    - tests/shadow/src/bin/matrix_test_client.rs contains `CsApi {` and `E2eeMessaging {` and `SasVerify {`
    - tests/shadow/src/bin/matrix_test_client.rs contains `--role` for each new variant
    - Existing `Smoke` variant is unchanged
    - `cargo check -p shadow-test-harness` exits 0
  </acceptance_criteria>
  <done>Multi-host Shadow topology builder exists. TuwunelConfig has encryption field. Binary accepts cs-api, e2ee-messaging, and sas-verify subcommands with --server-url and --role args. All compiles clean.</done>
</task>

</tasks>

<verification>
- `cargo check -p shadow-test-harness` succeeds (matrix-sdk + all new code compiles)
- `cargo build --profile shadow -p shadow-test-harness --bin matrix-test-client` succeeds
- The three_host_config function produces valid YAML when serialized (can verify in Plan 02 integration test)
</verification>

<success_criteria>
- matrix-sdk 0.16 compiles within the workspace despite [patch.crates-io] entries
- Scenario module scaffold exists with common helpers (create_sdk_client, register_with_token, login_user, wait_for_server)
- Multi-host Shadow config builder (three_host_config) generates configs with server + alice + bob hosts
- matrix_test_client binary has cs-api, e2ee-messaging, sas-verify subcommands (stubs)
- All existing smoke test code is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-cs-api-and-e2ee-tests/02-01-SUMMARY.md`
</output>
